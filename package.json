{
  "name": "@frankx-ai/agent-intelligence-system",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic Intelligence System — AEO/GEO substrate, MCP server, marketplace, defense intel. Sibling to SIS/Library OS/IIS/SBO.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=24" },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "build": "pnpm -r build && node scripts/generate-assets.mjs"
  },
  "devDependencies": {
    "typescript": "5.6.2",
    "vitest": "2.1.1",
    "@types/node": "24.0.0"
  }
}
